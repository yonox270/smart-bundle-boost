import { Session, ShopifyRestResources } from '@shopify/shopify-api';
import type { AppConfigArg } from '../../config-types';
import { BasicParams } from '../../types';
import type { AdminApiContext } from './types';
interface RestClientOptions {
    params: BasicParams;
    session: Session;
    handleClientError?: (error: any) => Promise<void>;
}
export declare function adminClientFactory<ConfigArg extends AppConfigArg, Resources extends ShopifyRestResources = ShopifyRestResources>({ params, handleClientError, session, }: RestClientOptions): AdminApiContext<ConfigArg, Resources>;
export {};
//# sourceMappingURL=factory.d.ts.map