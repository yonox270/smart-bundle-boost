'use strict';

var request = require('./request.js');
var query = require('./query.js');
var revoke = require('./revoke.js');

function scopesApiFactory(params, session, admin) {
    return {
        query: query.queryScopesFactory(params, session, admin),
        request: request.requestScopesFactory(params, session, admin),
        revoke: revoke.revokeScopesFactory(params, session, admin),
    };
}

exports.scopesApiFactory = scopesApiFactory;
//# sourceMappingURL=factory.js.map
